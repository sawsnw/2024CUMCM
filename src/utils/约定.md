# 工具函数约定
- 所有非核心工具函数分文件放在此目录下
- 日志工具统一使用 `logger.py` 中的 `setup_logger()` 函数
- 入口脚本示例：
  ```python
  from src.utils.logger import setup_logger

  logger = setup_logger(__name__, script_name="train_model")
  logger.info("脚本开始运行")
  ```