[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "test-project"
version = "0.1.0"
dependencies = [
    "numpy",
    "openpyxl",
    "matplotlib",
]

[tool.setuptools.packages.find]
include = ["src", "src.*"]


